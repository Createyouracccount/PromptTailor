# PromptTailor Benchmarks

재현 가능한 실측 벤치마크 모음. 모든 수치는 이 repo의 스크립트로 직접 재실행할 수 있고,
원본 모델 출력이 결과 파일에 보존된다. (요구사항: 로그인된 `claude` CLI)

## 1. Clarity Gate Benchmark — "재작성해야 하나, 놔둬야 하나"

**동기**: 작업 결과 파일럿(R34)에서 이미 명확한 요청을 재작성하면 결과가 오히려 나빠짐이 실측됨
(원문 3승 : 재작성 0승). 따라서 게이트의 판정 정확도가 이 도구의 안전성을 좌우한다.

**데이터셋**: [bench/gate_set.jsonl](bench/gate_set.jsonl) — 40건, 2클래스 균형:

| 클래스 | n | 출처 |
|---|---|---|
| `rewrite` (모호 — 재작성이 이득) | 20 | 골든셋 v01~v20 (재작성이 20/20 better로 실측된 프롬프트) |
| `keep` (명확 — 건드리면 손해) | 20 | 파일럿에서 원문이 승리한 3건 + 저자 작성 17건 (대상·목표·결과물이 명시된 요청) |

**실행**:

```bash
python3 bench/run_gate_bench.py --workers 5
```

**결과 — 반복 실행 안정성 포함 (2026-08-28, rewriter=claude-haiku-4-5, /pm 경로, 중립 cwd 3회)**:
원본: [runs/gate_stability/](runs/gate_stability/) (v2_run1~3.json), 원시 측정 로그: [runs/MEASUREMENT_LOG.md](runs/MEASUREMENT_LOG.md)

| 지표 | 값 (3회 범위) |
|---|---|
| **정확도** | **36–38/40 = 90–95%** (오류 0) |
| 모호 재현율 (rewrite→rewrite) | **59/60** — 3회 중 1회에서 1건 오판(v20), 나머지 전부 20/20 |
| 명확 재현율 (keep→keep) | **16–18/20** — 오판은 전부 keep→rewrite 방향 |
| 상시 오판 항목 | c02(CSV 스크립트) 3/3회, c12(fib 변환) 2/3회 — 짧지만 자기완결적인 과제의 경계 사례 |

**측정 이력 (정직하게)**: 최초 공개 수치는 **39/40 = 98%였으나 1회 실행·레포 안 cwd** 측정이었고,
중립 cwd 3회 재실측(2026-08-28)에서 88–90%로 재현되지 않았다. 오판 패턴(짧은 자기완결 과제를
"짧다=모호"로 판정)에 따라 게이트에 "길이가 아니라 완결성" 규칙을 추가한 뒤의 수치가 위 표(90–95%)다.
추가 문구 수정은 벤치마크 과적합 위험으로 중단했다. 교훈: 벤치마크는 항상 중립 cwd에서 3회 이상.

**한계 (정직하게)**:
- "명확함" 라벨은 저자 판단이다 — 실제 사용자 의도와 다를 수 있다.
- keep 클래스 17건은 저자 작성이라 분포가 실사용과 다를 수 있다. 실사용 로그 기반 확장이 다음 단계.
- 단일 재작성기(haiku). 판정은 회차 간 ±2건 흔들린다 — c02·c12 같은 경계 사례가 원인.

## 2. 기존 벤치마크 (골든셋·A/B — LOOP_LOG 원장 기록)

| 실험 | 스크립트 | 결과 |
|---|---|---|
| 프롬프트 품질 (모호 20건) | `eval/run_eval.py` | 20/20 better, clarity 5.0 · fidelity 4.8 · actionability 5.0 |
| 메타 언어 한/영 A/B | `eval/ab_meta_language.py` | 영어 우세 근거 없음 → 한국어 유지 (위치 편향 주의: R23) |
| intent 라우팅 v1/v2 A/B | `eval/ab_routing.py` | v2 4승 1패 1무 |
| 작업 결과 파일럿 (명확 3건) | `eval/ab_task_outcome.py` | **원문 3승 : 재작성 0승** (부정적 결과 — 게이트의 근거) |
| 토큰·비용 실측 | `eval/measure_cost.py` | 재작성 1회 ≈$0.03 · 훅 주입 +527토큰 |

## 관련 데이터셋·연구 (조사 결과, 2026-08-16)

이 벤치마크와 정확히 같은 목적(재작성/유지 게이트 라벨)의 공개 데이터셋은 찾지 못했다.
관련 연구로는 미명세 코딩 프롬프트를 다루는 [Curiosity by Design](https://arxiv.org/html/2507.21285v1)
(명확화 질문), [CAPA](https://arxiv.org/html/2607.26611)(개인화된 모호성 유형),
[UnderSpecBench](https://arxiv.org/html/2607.02294)(미명세 DevOps 지시의 행동 경계 위반),
[SWE-chat](https://arxiv.org/html/2604.20779v1)(실사용 코딩 에이전트 대화) 등이 있다.
HuggingFace에서는 prompt-optimization 계열 소규모 셋만 존재. 향후 실사용 프롬프트 로그(옵트인)로
keep 클래스를 확장하는 것이 데이터 품질 개선의 핵심이다.

## 방법론 원칙 (모든 벤치마크 공통)

1. 쌍대 비교는 블라인드 — 심판은 어느 쪽이 어느 조건인지 모른다 (배정 기록 후 해맹).
2. 제시 위치와 생성 순서를 분리 교차 (R23 심판이 잡은 위치 편향 재발 방지).
3. 심판·재작성기 원문 출력을 결과 파일에 보존 — 주장 재검증 가능.
4. 부정적 결과도 공개한다 — R34가 그 예다.
