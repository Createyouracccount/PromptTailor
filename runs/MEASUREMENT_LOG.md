# MEASUREMENT_LOG.md — 원시 측정 로그 (추가 전용)

측정이 실행될 때마다 결과를 **그대로** 여기에 추가한다 (최신이 아래). 해석·결정은 LOOP_LOG.md에, 확정된 주장만 README에 반영한다. 이 파일의 숫자는 수정하지 않는다 — 잘못된 측정이면 취소선을 긋고 사유를 남긴 뒤 재측정을 추가한다.

형식: 날짜 · 측정 항목 · 환경 · 원시 결과 · 원본 데이터 파일 경로

---

## 2026-08-28 · 게이트 반복 안정성 (R39, 3회 실행)

- 목적: BENCHMARK.md의 98%(1회 실행)가 반복 실행에서 안정적인지 — 판정이 흔들리는 항목 식별
- 환경: bench/gate_set.jsonl 40건 × 3회, target fable-5, concise, workers 4, 중립 cwd(레포 밖)에서 실행
- 결과: (실행 중 — 완료 시 기록)

## 2026-08-28 · Windows CI (R39)

- 목적: "Windows 미검증" 문구의 실측 상향
- 환경: GitHub Actions windows-latest + ubuntu-latest × py3.10/3.12, 오프라인 테스트 48건
- 결과: **4개 잡 전부 success** (run: CI "Run offline tests on Windows in CI") — Windows에서 오프라인 테스트 48/48 통과. E2E(claude CLI 포함)는 여전히 미검증 — README 문구를 그대로 이 사실로 교체

## 2026-08-28 · 재작성 지연 (R39 속도 라운드)

- 목적: 중앙값 10초 이하 달성 여부 판정. 기존 실측: 18–33s (R32), 원인 = `claude -p` 기동 오버헤드(입력 ~29.5k 토큰 중 ~99%가 CLI 시스템 프롬프트)
- 실험 A/B/C(eval/measure_latency.py, n=3/변형, haiku, 중립 cwd): A 현행 / B +`--system-prompt` 최소 교체 / C B+`--setting-sources ''`
- 결과(중앙값): **A 15.5s · B 19.2s · C 14.4s** — 원시: A 19.7/15.5/11.6, B 17.9/19.3/19.2, C 12.2/19.6/14.4 (runs/latency_experiment.json)
- **판정: 가설 기각(부정적 결과).** 시스템 프롬프트 교체·설정 생략 모두 유의미한 개선 없음(개별 실행 분산 11.6–19.7s가 변형 간 차이보다 큼) → CLI 경로 지연은 토큰량이 아니라 프로세스 기동·오케스트레이션이 지배. 현행 플래그 유지, 목표 "<10s"는 **CLI 기본 경로에서는 미달성**. 남은 레버는 옵트인 직접 API 경로(구현·오프라인 테스트 완료, 이 기기에 API 키 없어 **지연 미실측** — 실측 전까지 속도 주장 금지)
